import 'package:flutter/material.dart';

class LecSignup extends StatelessWidget {
  const LecSignup({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      height: 10,
      width: 10,
    );
  }
}