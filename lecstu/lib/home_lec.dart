import 'package:flutter/material.dart';

class HomeLec extends StatelessWidget {
  const HomeLec({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      color: Colors.red,
      height: 10,
      width: 10,
    );
  }
}